function key(scope, keyId, keyClass, keyStyle, fields, sortFn) {
	/*var keyId = keyId
	,	childKeys = []
	,	parentKey;

	return {
		keyId: keyId,
		childKeys: childKeys,
		parentKey: parentKey,
		sort: angular.isDefined(sortFn) ? sortFn: function(item) { return item;}
	}*/
}